import { coreStyles } from "./core";
import { deliveryStyles } from "./delivery";
import { editorStyles } from "./editor";

export const styles = {
  ...coreStyles,
  ...deliveryStyles,
  ...editorStyles,
};
